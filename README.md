# Loan Approval Prediction

> End-to-End ML-проект для прогнозирования одобрения кредита

[![Python](https://img.shields.io/badge/python-3.10+-blue.svg)](https://www.python.org/downloads/)
[![PyTorch](https://img.shields.io/badge/pytorch-2.0+-ee4c2c.svg)](https://pytorch.org/)
[![License: MIT](https://img.shields.io/badge/license-MIT-green.svg)](LICENSE)

## О проекте

Проект реализует полный цикл машинного обучения для задачи кредитного скоринга — от предобработки данных до интерпретации моделей. Включает сравнение классических методов (Random Forest, LightGBM) с нейронной сетью на PyTorch.

**Источник данных:** [Kaggle Playground Series s4e10](https://www.kaggle.com/competitions/playground-series-s4e10)
##  О датасете

Данные представляют собой типичную задачу кредитного скоринга:
для каждого заявителя указаны социально-демографические характеристики, доход, размер займа, тип недвижимости и пр.
Целевая переменная — **Loan_Status** (`Y`), бинарный признак: кредит одобрен или отклонён.


## Основные идеи

- **Унифицированный препроцессинг** — единый `ColumnTransformer` для всех моделей
- **Сравнение подходов** — классические алгоритмы vs нейросеть на одних данных
- **Стекинг моделей** — объединение предсказаний нескольких классификаторов
- **Интерпретируемость** — SHAP-анализ для понимания решений моделей
- **Модульность** — весь код вынесен в `src/`, ноутбук остаётся чистым
- **Воспроизводимость** — фиксированный random_state, конфиги в отдельном файле




## Результаты

Сравнение базовой логистической регрессии, лучших классических моделей и MLP
по валидации:

| Модель                | Balanced Accuracy | F1-macro | ROC-AUC |
|-----------------------|-------------------|---------:|--------:|
| Logistic Regression   | 0.88              |    0.86  |   0.93  |
| Random Forest         | 0.91              |    0.89  |   0.95  |
| LightGBM (best classic)| **0.93**        |  **0.91**| **0.96**|
| MLP (PyTorch)         | 0.90              |    0.88  |   0.94  |

Для Kaggle-скоринга (public/private leaderboard) лучшая модель — **LightGBM**:

| Модель   | Public Score | Private Score |
|----------|--------------|---------------|
| LightGBM | **0.95672**  | **0.95677**   |

### Почему LightGBM победил

LightGBM лучше всего справился с нелинейными зависимостями и взаимодействиями
признаков (доход, возраст, доля займа от дохода и т.д.) при ограниченном
числе признаков и умеренном размере выборки. За счёт градиентного бустинга
по деревьям он даёт более высокую ROC-AUC и F1-macro по сравнению с
логистической регрессией и MLP на тех же данных.

## Стек

| Категория | Технологии |
|-----------|------------|
| ML | scikit-learn, LightGBM |
| DL | PyTorch |
| Интерпретация | SHAP |
| Данные | pandas, numpy |

## Структура

Проект организован модульно, чтобы отделить логику от экспериментов:

```
├── data/                       # Сырые данные (train.csv, test.csv)
├── src/                        # Исходный код проекта
│   ├── classic_ml/             # Классические модели
│   │   ├── pipeline.py         # Сборка sklearn-пайплайнов
│   │   ├── grid_search.py      # Подбор гиперпараметров
│   │   ├── staking.py          # Реализация стекинга
│   │   └── evaluate_pipes.py   # Оценка качества моделей
│   ├── data_and_preprocessors/ # Работа с данными
│   │   ├── data.py             # Загрузка и сплит данных
│   │   └── preprocessors.py    # ColumnTransformer и пайплайны обработки
│   ├── mlp/                    # Нейросеть (PyTorch)
│   │   ├── mlp_model.py        # Архитектура MLP
│   │   ├── train_mlp.py        # Цикл обучения
│   │   └── mlp_prepare_data.py # Подготовка DataLoader'ов
│   └── shap_utils/             # Интерпретация (SHAP)
├── config.py                   # Константы (пути, random_state, параметры обучения)
├── main.ipynb                  # Основной сценарий эксперимента (end-to-end)
└── requirements.txt            # Зависимости проекта
```

## Быстрый старт

```bash
git clone https://github.com/shtapic/loan-approval-ml-pipeline.git
cd loan-approval-ml-pipeline
python -m venv .venv && .\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

Затем откройте `main.ipynb` и выполните ячейки.

## Модели

**Classic ML:**
- Logistic Regression — baseline
- Random Forest
- LightGBM
- Stacking Classifier

**Deep Learning:**
- MLP с BatchNorm и Dropout
- BCEWithLogitsLoss с балансировкой классов




## 📈 Метрики

| Метрика                       | Описание                                                                                                                                                                            | Интерпретация                                      |
| ----------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------- |
| **Balanced Accuracy**         | Среднее значение полноты (Recall) для каждого класса. Компенсирует дисбаланс классов, показывая, насколько хорошо модель распознаёт как положительные, так и отрицательные примеры. | 1.0 — идеальная классификация; 0.5 — угадывание.   |
| **F1-macro**                  | Среднее гармоническое между precision и recall, усреднённое по классам. Не чувствительно к дисбалансу, отражает баланс между «чистотой» и «полнотой» предсказаний.                  | Чем выше, тем лучше; максимум = 1.                 |
| **ROC-AUC**                   | Площадь под ROC-кривой (True Positive Rate vs False Positive Rate). Показывает способность модели различать классы независимо от выбранного порога.                                 | 1 — идеально; 0.5 — случайное угадывание.          |
| **Average Precision (macro)** | Площадь под PR-кривой (Precision-Recall), усреднённая по классам. Более показательная при сильном дисбалансе классов.                                                               | 1 — идеальный precision/recall; ближе к 0 — плохо. |
| **Log-loss**                  | Средняя логарифмическая потеря между вероятностями предсказания и истинными метками. Строго наказывает за уверенные, но неверные прогнозы.                                          | Чем меньше, тем лучше; идеал = 0.                  |


Для бинарной классификации: 

```python
roc_auc_score(y_true, model.predict_proba(X)[:, 1])
```


## SHAP

Для всех основных моделей реализован SHAP-анализ, который помогает понять,
какие признаки сильнее всего влияют на решение модели:

- `TreeExplainer` — для деревьев (Random Forest, LightGBM)
- `LinearExplainer` — для логистической регрессии  
- `DeepExplainer` — для нейросети (MLP на PyTorch)

В ноутбуке `main.ipynb` есть примеры генерации **SHAP summary plot** для
классических моделей и MLP.

## Лицензия

MIT

